export const defaultRegion = 'us-central1';
export const defaultZone = `${defaultRegion}-b`;
export const defaultProject = 'acme-dev';
export const internalDomain = 'acme.org';

export const providerDefaults = {
  zone: defaultZone,
  project: defaultProject
};
